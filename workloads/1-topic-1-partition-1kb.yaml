name: Consumer Scaling Test

topics: 1
partitionsPerTopic: 10       # 足够的分区支持多个consumer
keyDistributor: "NO_KEY"
messageSize: 1024            # 直接用messageSize生成payload

producersPerTopic: 1
producerRate: 1000          # 总共 40K msg/s

# 测试不同Consumer数量
consumerPerSubscription: 10
subscriptionsPerTopic: 1     # 单个consumer group

testDurationMinutes: 1
warmupDurationMinutes: 1
